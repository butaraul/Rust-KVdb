[package]
name = "persistence"
description = "Write-ahead log and snapshot persistence for the storage engine"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
storage = { path = "../storage" }
protocol = { path = "../protocol" }
tracing = "0.1"

[dev-dependencies]
tempfile = "3"
