[package]
name = "storage"
description = "From-scratch B-Tree storage engine over a custom arena allocator"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
rand = "0.8"

[[bench]]
name = "btree_bench"
harness = false
